<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cart Page</title>
    <link rel="stylesheet" href="/stylesheets/styles.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="flex flex-row justify-between">
        <h2 class="text-blue-400 font-bold text-2xl">SHOP</h2>
        <% if(success.length > 0) { %>
            <div id="success" class="text-green-500"><%= success %></div>
        <% } %>
        <% if(error.length > 0) { %>
            <div id="error" class="text-red-500"><%= error %></div>
        <% } %>
    </div>
    <div class="flex flex-col items-center justify-center min-h-screen text-center">
        <h2>Thanks for your purchase!</h2>
        <p>Your order has been placed successfully. We will notify you once it is shipped.</p>
        <a class="mt-3 text-blue-400 hover:underline text-sm" href="/dashboard/data">Back to Dashboard</a>
    </div>
    <script src="/javascripts/flashScript.js"></script>
</body>
</html>