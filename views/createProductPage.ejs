<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <link rel="stylesheet" href="/stylesheets/styles.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="flex flex-row justify-between">
        <h2 class="text-blue-400 font-bold text-2xl">SHOP</h2>
        <% if(success.length > 0) { %>
            <div id="success" class="text-green-500"><%= success %></div>
        <% } %>
        <% if(error.length > 0) { %>
            <div id="error" class="text-red-500"><%= error %></div>
        <% } %>
        <a class="mt-3 text-blue-400 hover:underline text-sm" href="/dashboard">Back to Dashboard</a>
    </div>
    <div class="p-[15rem]">
        <form class="flex flex-col gap-2" action="/product/create" method="POST" enctype="multipart/form-data">
            <div class="flex flex-row">
                <label class="w-[7rem]" for="name">Product Name</label>
                <input class="border-1 border-blue-400 outline-none px-2 py-1 rounded-lg" type="text" id="name" name="name" required>
            </div>
            <div class="flex flex-row">
                <label class="w-[7rem]" for="price">Price</label>
                <input class="border-1 border-blue-400 outline-none px-2 py-1 rounded-lg" type="number" id="price" name="price" min="0" required>
            </div>
            <div class="flex flex-row">
                <label class="w-[7rem]" for="discount">Discount</label>
                <input class="border-1 border-blue-400 outline-none px-2 py-1 rounded-lg" type="number" id="discount" name="discount" min="0">
            </div>
            <div class="flex flex-row">
                <label class="w-[7rem]" for="category">Category</label>
                <select class="border-1 border-blue-400 outline-none px-2 py-1 rounded-lg" id="category" name="category" required>
                    <option value="other" selected>other</option>
                    <option value="electronics">electronics</option>
                    <option value="accessories">accessories</option>
                    <option value="clothes">clothes</option>
                </select>
            </div>
            <div class="flex flex-row">
                <label class="w-[7rem]" for="images">Images</label>
                <input class="border-1 border-blue-400 outline-none px-2 py-1 rounded-lg" type="file" id="images" name="images" required multiple>
            </div>
            <div class="ml-[3rem]">
                <input class="w-[12rem] rounded-md bg-blue-400 hover:bg-blue-500 mt-3" type="submit" value="Create Product">
            </div>
        </form>
    </div>
    <script src="/javascripts/flashScript.js"></script>
</body>
</html>