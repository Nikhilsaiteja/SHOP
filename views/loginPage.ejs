<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <link rel="stylesheet" href="/stylesheets/styles.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="flex flex-row justify-between">
        <h2 class="text-blue-400 font-bold text-2xl">SHOP</h2>
        <% if(success.length > 0) { %>
            <div id="success" class="text-green-500"><%= success %></div>
        <% } %>
        <% if(error.length > 0) { %>
            <div id="error" class="text-red-500"><%= error %></div>
        <% } %>
    </div>
    <div class="p-[15rem]">
        <form class="flex flex-col gap-2" action="/user/login" method="POST">
            <div class="flex flex-row">
                <label class="w-[7rem]" for="email">Email</label>
                <input class="border-1 border-blue-400 outline-none px-2 py-1 rounded-lg" type="email" id="email" name="email" required>
            </div>
            <div class="flex flex-row">
                <label class="w-[7rem]" for="password">Password</label>
                <input class="border-1 border-blue-400 outline-none px-2 py-1 rounded-lg" type="password" id="password" name="password" required>
            </div>
            <div class="flex flex-row gap-2">
                <input class="w-[7rem] rounded-md bg-blue-400 hover:bg-blue-500 mt-3" type="submit" value="Login">
                <a class="mt-3 text-blue-400 hover:underline text-sm" href="/user/registration">Don't have an account? Register</a>
            </div>
        </form>
    </div>
</body>
</html>